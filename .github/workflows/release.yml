name: release

on:
  push:
    tags:
      - 'v*.*.*'

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write  # nodig voor softprops/action-gh-release
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install PlatformIO
        run: pip install platformio

      - name: Cache PlatformIO toolchains
        uses: actions/cache@v4
        with:
          path: |
            ~/.platformio/.cache
            ~/.platformio/packages
            ~/.platformio/platforms
          key: pio-${{ runner.os }}-${{ hashFiles('platformio.ini') }}

      - name: Build firmware
        run: pio run

      - name: Verify version matches tag
        run: |
          TAG_VERSION="${GITHUB_REF_NAME#v}"
          FW_VERSION=$(grep FIRMWARE_VERSION platformio.ini | grep -oE '[0-9]+\.[0-9]+\.[0-9]+' | head -1)
          if [[ "$TAG_VERSION" != "$FW_VERSION" ]]; then
            echo "Tag $GITHUB_REF_NAME komt niet overeen met FIRMWARE_VERSION=$FW_VERSION in platformio.ini" >&2
            exit 1
          fi
          echo "VERSION=$FW_VERSION" >> "$GITHUB_ENV"

      - name: Stage artefacten
        run: |
          mkdir -p release
          # V3 (ESP32 + LAN8720)
          cp .pio/build/smarthomeshop-v3/firmware.bin        release/firmware-v3.bin
          cp .pio/build/smarthomeshop-v3/firmware-merged.bin release/firmware-merged-v3.bin
          sha256sum release/firmware-v3.bin | awk '{print $1}' > release/firmware-v3.bin.sha256

          # V4 (ESP32-C6 + W5500)
          cp .pio/build/smarthomeshop-v4/firmware.bin        release/firmware-v4.bin
          cp .pio/build/smarthomeshop-v4/firmware-merged.bin release/firmware-merged-v4.bin
          sha256sum release/firmware-v4.bin | awk '{print $1}' > release/firmware-v4.bin.sha256

          BASE="https://github.com/${{ github.repository }}/releases/download/${{ github.ref_name }}"

          cat > release/manifest.json <<EOF
          {
            "name": "SlimHuys P1-bridge",
            "version": "${VERSION}",
            "new_install_prompt_erase": true,
            "builds": [
              {
                "chipFamily": "ESP32",
                "parts": [
                  { "path": "${BASE}/firmware-merged-v3.bin", "offset": 0 }
                ]
              },
              {
                "chipFamily": "ESP32-C6",
                "parts": [
                  { "path": "${BASE}/firmware-merged-v4.bin", "offset": 0 }
                ]
              }
            ]
          }
          EOF

          ls -lh release/

      - name: Publish GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            release/firmware-v3.bin
            release/firmware-v3.bin.sha256
            release/firmware-merged-v3.bin
            release/firmware-v4.bin
            release/firmware-v4.bin.sha256
            release/firmware-merged-v4.bin
            release/manifest.json
          generate_release_notes: true
